apiVersion: v1
kind: Service
metadata:
  name: jeu-web-service
spec:
  type: NodePort
  selector:
    app: jeu-web
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30030